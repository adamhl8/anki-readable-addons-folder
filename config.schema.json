{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Readable Addons Folder",

  "type": "object",
  "properties": {
    "folderName": {
      "title": "Folder Name",
      "type": "string",
      "description": "The name of the folder which will contain the symlinks."
    },
    "containingFolder": {
      "title": "Parent Folder",
      "type": "string",
      "description": "Absolute path to the folder in which the readable addons folder is made. It must already exist.",
      "ui:widget": "dirpath"
    }
  }
}
